package com.signalscope.app

import android.app.Application

class SignalScopeApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // App-level initialization if needed
    }
}
