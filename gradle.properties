org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.dependency.excludeLibraryComponentsFromConstraints=true
android.r8.strictFullModeForKeepRules=false

# These flags are deprecated in AGP 9.x but removing them breaks the build
# (builtInKotlin=false prevents classcast conflict between AGP and KGP;
#  the others are required for the AGP 9.1.1 + KGP 2.2.x DSL init order).
# Leave them in place until a future AGP update resolves the incompatibility.
android.builtInKotlin=false
android.newDsl=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.defaults.buildfeatures.resvalues=true
android.r8.optimizedResourceShrinking=false
